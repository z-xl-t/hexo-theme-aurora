<% if (post.tags && post.tags.length){ %>
  <span class="tags">
    <i class="icon icon-tag"></i>
      <%- list_tags(post.tags, {
        show_count: false,
        class: 'tag',
        style: 'none',
        separator: '、',
        amount: amount
      }) %>
  </span>
<% } %>