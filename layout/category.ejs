<div id="categories">
  <div class="card">
    <%- partial('_partial/quote.ejs', {enable: theme.quotes.categories.enable, quote: theme.quotes.categories.quote}) %>
    <%- partial('_partial/archive.ejs', {posts: page.posts}) %>
    <ul class="content">
      <% if (site.categories.length === 0 ){ %>
        <li class="message"><span>暂无分类</span></li>
      <% } else { %>
        <%- partial('_partial/category-header.ejs') %>
      <% } %>
    </ul>
  </div>
</div>